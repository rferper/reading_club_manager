[project]
name = "reading-club-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rferper", email = "raquelfernandezperalta@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
reading-club-manager = "reading_club_manager:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

"""The club's test suite, one module per area.

Django's runner discovers any file matching `test*.py` under here, so a
new area is a new module and nothing else — see
`_docs/testing-guidelines.md`.
"""
